---
name: baemin-ops-prd
description: 배민 사장님이 가게 운영 도구(급여·근무표·매뉴얼·원가·정산·재고·매출 대시보드·날씨 준비·체크리스트 등)를 바이브 코딩으로 만들기 전에 기획안(PRD)을 써야 할 때 사용한다. "PRD 써 줘", "기획안 도와줘", "전체 PRD", "v1 v2 나눠 줘", "PRD_전체 채워 줘", "사전 과제 PRD 옮겨 줘", "v1 써 봤는데 v2 다듬자", "중간 점검 준비", "배민 옵스 PRD" 같은 말, 또는 배민 실전 캠프 저장소(baemin-ops-os-camp)의 PRD_전체.md·v1/PRD.md·v2/PRD.md·사용메모.md를 다루는 상황에서 반드시 사용한다. 답이 짧거나 "빨리 해 줘"라고 해도, 주제가 운영이 아니어도(인스타 캐러셀·상품 기획·강의 등) 사용한다. 이미 PRD가 확정돼 구현만 남은 요청에는 쓰지 않는다.
---

# 배민 옵스 PRD — 가게 운영 도구 기획 인터뷰

## 핵심 원칙

**전체를 먼저 그리고, 나누고, 써 본 뒤 다시 고친다.**
`PRD_전체.md`(다 만들었을 때의 모습) → `v1/PRD.md`(오늘 만들 것) + `v2/PRD.md`(초안) → v1을 써 본 기록(`사용메모.md`)으로 v2를 고친다.

처음 기획이 단답형이면 만든 뒤에 고칠 게 쌓인다. 이 스킬의 일은 **사장님이 짧게 말한 것을 실제 장면·숫자·예외까지 끌어내는 것**이다.

**규칙의 글자를 어기는 것은 규칙의 취지를 어기는 것이다.**

## 시작할 때 (매번)

1. 스킬 최신화: `git -C ~/.claude/skills/baemin-ops-prd pull --ff-only -q 2>/dev/null` (실패해도 계속)
2. 상태 판별: `bash ~/.claude/skills/baemin-ops-prd/scripts/detect.sh "$PWD"` (폴더는 절대 경로로) → `references/00-detect.md`로 모드를 정한다.
3. 캠프 저장소면 먼저 `git pull`. 내 폴더(사장님 폴더)가 어딘지 모르면 가게 이름이나 사장님 이름을 묻는다.
4. 모드가 확실하면(표의 조건이 하나만 맞음) 묻지 않고 "○○로 시작할게요"라고 알린다. 애매할 때만 추천을 첫 선택지로 묻는다.
5. 이 스킬이 맞는 요청이면 다른 기획 스킬(brainstorming·oneday-prd·sponge-ops-os 등)을 겹쳐 부르지 않는다. 이 스킬의 인터뷰가 그 역할이다.

| 모드 | 언제 | 읽을 것 |
|---|---|---|
| ① 처음 쓰기 | 전체·v1 PRD가 빈 양식 | `10-interview.md` `20-split.md` `50-topics.md` `40-handoff.md` |
| ② 이어 쓰기 | v1 PRD가 채워져 있거나, 사전 과제 PRD·아티팩트를 붙여넣음 | `10-interview.md`(「이어 쓰기」) `20-split.md` `40-handoff.md` |
| ③ v2 고도화 | v1을 **만들어 써 본 근거**가 있음: 사장님이 써 봤다고 함 · 사용메모에 기록 · 중간 점검 준비. "v2"라는 말만으로는 ③이 아니다 | `30-revisit.md` `10-interview.md`(「파고들기」) `20-split.md` `40-handoff.md` |

## 인터뷰 규칙 (모드 ①②)

- **최대 30분.** 첫 인터뷰 질문부터 잰다(모드·폴더 확인, 전체 PRD 보여 주기와 확인, 파일 쓰기는 빼고). 시작할 때 "30분 안에 끝낼게요"라고 말한다. 모드 ②는 "15분쯤, 길어도 30분"이라고 말한다. 블록별 시간은 `10-interview.md`.
- **한 번에 한 질문.** 선택지 2~4개 + 추천을 첫 번째에. 사장님이 직접 말할 칸은 항상 열어 둔다.
- **짧은 답은 판다.** "편하게", "자동으로", "관리", "그냥" 같은 답이나 한 단어 답이 오면 다음 질문으로 넘어가지 않고 셋 중 하나로 다시 묻는다: *"예를 들면요?"* / *"지난주에 실제로는 어땠어요?"* / *"몇 번·몇 분·몇 명이에요?"* 한 칸에서 되묻기는 **최대 두 번**, 그래도 짧으면 `[가정]`으로 적고 11번에도 옮긴다.
- **질문과 상관없는 짧은 답**(인원을 물었는데 "자동으로")은 원래 질문을 선택지로 다시 묻고(되묻기 횟수에 세지 않는다), 그 말은 해당 블록에서 팔 거리로 적어 둔다.
- **"빨리"는 질문을 줄이는 이유가 아니다.** 선택지를 더 촘촘히 줘서 빨리 답하게 한다. 블록의 채움 기준을 못 채운 채 넘기는 건 30분이 다 됐을 때뿐이고, 그때 남은 칸은 `[가정]`으로 쓰고 11번(아직 정하지 못한 것)에 옮긴다.
- **블록이 끝날 때마다 되짚기.** "제가 이해한 건 이거예요" 3~5줄 → 틀린 곳만 고친다.
- 저장소·가게 소개·기존 PRD로 알 수 있는 건 묻지 않고 확인만 한다.

## 주제가 운영에서 벗어날 때

오늘 만드는 것은 **가게 운영 도구** = 사장님·직원이 가게를 굴리며 하는 일(직원·급여·교육·재고·원가·정산·매출 보기·준비·손님 응대와 안내·예약/주문 처리)을 덜어 주는 도구다.
운영이 **아닌** 것 = 홍보 콘텐츠 자체 제작(캐러셀·릴스·블로그 글), 상품·메뉴 기획 자체, 판매용 강의·콘텐츠 제작. 이런 주제를 말하면 그대로 기획하지 않는다.

1. "오늘은 가게 운영을 돕는 도구를 만드는 날이라, 운영 도구로 해 볼게요"라고 한 번 안내한다.
2. 원래 하고 싶던 것과 **이어지는 운영 도구 2~3개**를 선택지로 준다. 예시는 `references/50-topics.md` 「이어 주기」.
3. 첫 선택지(추천): 개별 주제가 있으면 개별 주제, 없으면 원래 아이디어와 가장 가까운 조 주제.
4. 운영 도구가 정해지면, 운영이 아닌 원래 주제는 PRD_전체 11번에 "나중에 따로"로 적어 둔다. 운영 도구가 정해지지 않은 채 끝나면 파일을 만들지 않는다.

"원래 것을 그대로 만들기"는 선택지로 주지 않는다. "직접 말하기" 칸에는 "(운영 도구로)"를 붙인다. 사장님이 그래도 원하면 **한 번 더** 이유(오늘 과제가 운영 도구)와 이어지는 선택지를 주고, 세 번째에도 원하면 강사·운영진과 먼저 상의하시라고 안내하고 기다린다. 운영이 아닌 주제로 PRD를 쓰지 않는다.

이미 써 온 v1 PRD(모드 ②)가 경계에 걸치면(예: 손님이 쓰는 메뉴 안내) 위 정의로 판단하고, 운영에 들면 그대로 이어 쓴다.

## 산출물

| 파일 | 내용 | 기준 |
|---|---|---|
| `PRD_전체.md` | 12칸. 4번 기능마다 [v1]/[v2]/[나중] + 나눈 이유 | 먼저 쓴다 |
| `v1/PRD.md` | [v1]만. 8칸 양식 | `20-split.md` v1 기준 |
| `v2/PRD.md` | [v2]만. 제목 바로 아래에 초안 표시(문구는 `20-split.md`) | 모드 ③에서 확정 |

양식은 캠프 폴더의 `_양식/`이 있으면 그것을, 없으면 `templates/`를 쓴다. 모드 ①②는 두 번의 확인을 받는다: **전체 PRD를 보여 준 뒤**, **v1/v2로 나눈 뒤**. (모드 ③의 확인은 `30-revisit.md`)

이미 있는 `v1/PRD.md`(모드 ②)는 덮어쓰지 않는다. 나눈 결과와 다른 줄만 맞추고, 사장님이 적은 계획을 바꾸게 되면(예: 사장님이 v2에 둔 것을 [나중]으로) 그 줄을 콕 집어 묻는다.

끝나면 `references/40-handoff.md`대로: 내 폴더만 커밋·push → v1 구현용 프롬프트 → 사용메모 안내.

## 자주 나오는 핑계

| 핑계 | 실제 |
|---|---|
| "사장님이 시간 없다고 하셨으니 질문을 줄이자" | 줄일 건 질문 수가 아니라 답하는 수고다. 선택지로 빨리 받는다. 30분 상한이 이미 시간 약속이다. |
| "짧은 답도 답이니 기본값으로 채우자" | 기본값은 30분이 다 됐을 때만. 그 전엔 한 번 더 판다. |
| "원래 하고 싶은 걸 존중해서 선택지로 주자" | 오늘의 과제는 운영 도구다. 이어지는 운영 도구로 존중한다. |
| "v1만 확실하면 되니 전체 PRD는 간단히" | 전체가 흐리면 v2 재료가 없다. 전체 4번 표가 v1·v2의 원본이다. |
| "v2도 지금 확정해 두자" | v2는 써 본 뒤에 바뀌는 게 정상이다. 오늘은 초안. |
| "잘 써 온 분은 숫자만 물으면 된다" | 미리 채운 전체 PRD를 보여 주고 블록마다 "더할 게 있나요?"까지 묻는다. |
| "사장님이 v2라고 했으니 고도화 모드" | v1을 만들어 써 본 근거가 없으면 모드 ②다. |
| "두 번 거절하셨으니 원래 주제로 써 드리자" | 운영이 아닌 주제로 PRD를 쓰지 않는다. 운영진 상의를 안내한다. |

## 멈춤 신호

- 한 단어 답을 받고 바로 다음 블록으로 넘어가려 할 때
- 실제로 있었던 장면(1번) 없이 기능 목록부터 쓰고 있을 때
- 운영이 아닌 주제로 PRD를 쓰기 시작했을 때
- 전체 PRD 없이 v1/PRD.md를 새로 쓰거나 고치고 있을 때
- 다른 사장님 폴더나 공용 파일을 고치려 할 때

## 하지 않는 것

- 코드 구현. 이 스킬은 기획까지다. 구현은 `40-handoff.md`의 프롬프트로 넘긴다.
- 실제 직원·손님 이름, 연락처, 실제 매출 파일, 비밀번호·API 키를 PRD에 적기.
- 급여 확정·세무·노무 판단을 대신하기. "확인 필요"로 적는다.
